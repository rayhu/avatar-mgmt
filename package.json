{
  "name": "avatar-mgmt",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "engines": {
    "node": ">=22.0.0",
    "yarn": "1.x"
  },
  "packageManager": "yarn@1.22.22",
  "lint-staged": {
    "frontend/**/*.{js,ts,vue}": [
      "cd frontend && yarn lint",
      "cd frontend && yarn format"
    ],
    "frontend/**/*.{json,md}": [
      "cd frontend && yarn format"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.0",
    "@vercel/node": "^3.0.2"
  },
  "volta": {
    "node": "22.16.0",
    "yarn": "1.22.22"
  }
}
