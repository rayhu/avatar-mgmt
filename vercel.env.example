# Vercel 环境变量配置示例
# 复制此文件为 .env.local 并在 Vercel 中设置

# 应用配置
NODE_ENV=production
BUILD_ENVIRONMENT=production

# 共享 Directus 配置
DIRECTUS_URL=https://directus.daidai-preview.amis.hk
DIRECTUS_TOKEN=your-directus-token

# Azure TTS 配置
AZURE_SPEECH_KEY=your-azure-speech-key
AZURE_SPEECH_REGION=southeastasia

# OpenAI 配置
OPENAI_API_KEY=your-openai-api-key

# 认证配置（如果需要认证功能）
# 注意：不同环境应该使用不同的 JWT_SECRET
JWT_SECRET=your-jwt-secret-here

# 日志配置（可选，有默认值）
LOG_LEVEL=info
LOG_FORMAT=json

# 文件上传配置（可选，有默认值）
MAX_FILE_SIZE=10485760  # 10MB
UPLOAD_DIR=uploads
