{
  "version": 2,
  "name": "avatar-mgmt",
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    },
    {
      "src": "api-server/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api-server/index.ts"
    },
    {
      "src": "/health",
      "dest": "/api-server/index.ts"
    },
    {
      "src": "/(.*)",
      "dest": "/frontend/$1"
    }
  ],
  "regions": ["hkg1"],
  "env": {
    "NODE_ENV": "production",
    "VITE_API_BASE_URL": ""
  },
  "build": {
    "env": {
      "NODE_ENV": "production",
      "VITE_API_BASE_URL": ""
    }
  }
}
