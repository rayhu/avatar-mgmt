# Stop on first failure
set -e

# Ensure repo is clean and install is up-to-date (optional – skip for speed)
# Uncomment if you want to verify lockfile consistency
# yarn install --immutable

# 1. Lint & type-check frontend
cd frontend

# Lint & type check (package.json script "check")
yarn run check

# 2. Verify i18n keys completeness
yarn i18n:check

# 3. (Optional) run unit tests / build – uncomment if required
# yarn test
# yarn build
