# Vercel 环境配置
# 用于所有 Vercel 部署环境

# 应用配置
NODE_ENV=production
BUILD_ENVIRONMENT=vercel

# 共享 Directus 配置
DIRECTUS_URL=${DIRECTUS_URL} # https://directus.daidai-preview.amis.hk
DIRECTUS_TOKEN=${DIRECTUS_TOKEN}

# Azure TTS 配置
AZURE_SPEECH_KEY=${AZURE_SPEECH_KEY}
AZURE_SPEECH_REGION=${AZURE_SPEECH_REGION}

# OpenAI 配置
OPENAI_API_KEY=${OPENAI_API_KEY}

# 认证配置（如果需要认证功能）
JWT_SECRET=${JWT_SECRET}

# 日志配置
LOG_LEVEL=info
LOG_FORMAT=json

# 文件上传配置
MAX_FILE_SIZE=10485760  # 10MB
UPLOAD_DIR=uploads

# 环境标识
PLATFORM=vercel
INSTANCE_TYPE=serverless
